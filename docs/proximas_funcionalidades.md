- Que los archivos se guarden en una carpeta, fuera del directorio donde se ejecuta cargo run, osea afuera por si se hace un git push origin master
- Que el programa enumere automaticamente las descargas, por ejemplo si se descargo "transcript_vide_0" el proximo que sea nombrado "transcript_vide_01"
    - Que identifique si ya existe una archivo igual o con el mismo nombre, si pasa renombrar automaticamente y avisar por consola la modificacion.
- Avisar por consola si ya descargo la transcripcion, considerando un historial de URL y como se llamo el archivo.
- PREGUNTAS
    
    - Si empaqueto y ejecuto en otra computadora funcionara?